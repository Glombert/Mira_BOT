# LLM
openai>=1.0.0
anthropic>=0.25.0

# Переменные окружения
python-dotenv>=1.0.0

# Excel (понадобится в Этапе 3)
openpyxl>=3.1.0

# Web backend (Этап 4)
fastapi>=0.110.0
uvicorn>=0.29.0

# Telegram bot (Этап 4)
python-telegram-bot>=21.0

# Векторная память (Этап 5, пока не нужно)
# chromadb>=0.4.0
