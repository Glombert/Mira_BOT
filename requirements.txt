# LLM
openai>=1.0.0
anthropic>=0.25.0

# Веб-поиск (Scout)
ddgs>=6.0.0

# Шифрование памяти
cryptography>=42.0.0

# Переменные окружения
python-dotenv>=1.0.0

# Excel
openpyxl>=3.1.0

# Telegram bot
python-telegram-bot>=22.0

# Веб-интерфейс (Этап 9)
fastapi>=0.110.0
uvicorn>=0.29.0
websockets>=12.0
python-multipart>=0.0.9
# Security: transitive deps with CVE fixes
starlette>=1.0.1
idna>=3.15

# Семантическая память (v1.3)
chromadb>=0.5.0

# Google Drive OAuth (v1.4)
google-auth-oauthlib>=1.0.0
google-api-python-client>=2.0.0
google-auth-httplib2>=0.2.0

# Firebase Cloud Messaging (push в мобайл)
firebase-admin>=6.5.0

# Rituals scheduling
croniter>=2.0.0

# Наблюдаемость — отправка ошибок в Sentry (опционально, активируется SENTRY_DSN)
sentry-sdk>=2.0.0

# Тесты (dev)
pytest>=8.0.0

# v2ray_api stats (per-user Reality-трафик)
grpcio>=1.60
