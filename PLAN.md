# Mira_BOT — План разработки

> Версия: 2.3
> Последнее обновление: 2026-05-04
> Архитектура: см. ARCHITECTURE.md

---

## Текущее состояние

- [x] Базовый агент с историей диалога
- [x] Multi-provider: DeepSeek, OpenAI и любой OpenAI-совместимый
- [x] Git sync (`/git`)
- [x] Рефлексия (`/reflect`) — агент читает свой код
- [x] Эволюция (`/evolve`) — агент предлагает патч к коду
- [x] Ротируемые логи
- [x] **Этап 0.1** — структура папок и файлов
- [x] **Этап 0.2** — защита саморедактирования (бэкап, валидация, smoke-test, rollback)
- [x] **Этап 0.3** — профили пользователей (Profile класс, --profile аргумент)
- [x] **Этап 0.4** — рефакторинг конфигурации (все пути в одном месте)
- [x] **Баг-фикс** — 5 проблем найденных при code review:
  - `import tempfile` перенесён в начало файла
  - `git add .` заменён на явный список безопасных файлов
  - diff в `/evolve` показывается целиком с пагинацией
  - `setup_client` обёрнут в try/except в `/switch`
  - `trim_history` вызывается при добавлении в messages
- [x] **`.profile` файл** — агент читает профиль по умолчанию из `.profile`, не нужно писать `--profile dev` каждый раз
- [x] **Этап 0.5** — память пользователя и онбординг (`identify_user`, `run_onboarding`, `/whoami`, `/forget`, `cleanup_temp`, workspace-структура)

---

## Принятые архитектурные решения

### Самоизменение и пользователи
- `agent.py` — единое ядро для всех пользователей. Мира не меняет ядро под конкретного пользователя.
- `/evolve` — инструмент только для профиля `dev`. В остальных профилях заблокирован.
- Мира меняет: конфиги агентов в `agents/`, память пользователя в `memory/`.
- Новые агенты в Конклав (`agents/новый.json`) создаются Альфой — это не изменение ядра.

### Облачное хранение памяти (rclone + Google Drive)
- Используем **rclone** (как в оригинальном Уроборосе) для синхронизации с Google Drive.
- Синхронизируется: `memory/` (профили, сессии), опционально `workspace/`.
- Команда `/cloud sync` — ручная синхронизация.
- Команда `/cloud restore` — восстановление на новом сервере.
- Настраивается в `.env`: `RCLONE_REMOTE=gdrive:mira_memory`
- Реализация в **Этапе 5** (долгая память).

### Структура workspace пользователя
```
workspace/
└── tg_123456789/
    ├── inbox/      ← пользователь кидает файлы сюда
    ├── output/     ← Мира кладёт результаты сюда
    └── temp/       ← временные файлы, автоочистка через 7 дней
```
- Мира работает только внутри `workspace/{user_id}/` — выход за пределы запрещён.
- Автоочистка `temp/` запускается при каждом старте агента.
- Реализация в **Этапе 0.5**.

---

## Целевая структура проекта

```
mira_bot/
│
├── agent.py                    # Ядро: класс Agent, главный цикл
├── conclave.py                 # Оркестратор Конклава
├── router.py                   # Классификатор и роутинг задач
├── .env                        # Ключи API (не в git!)
├── .profile                    # Профиль по умолчанию (например: dev)
├── .gitignore
├── requirements.txt
├── PLAN.md
├── ARCHITECTURE.md
│
├── agents/                     # Конфиги агентов
│   ├── _template.json
│   ├── alpha.json
│   ├── excel_specialist.json
│   ├── coder.json
│   ├── planner.json
│   └── critic.json
│
├── profiles/                   # Профили пользователей (настройки интерфейса)
│   ├── default.json
│   ├── dev.json
│   └── accountant.json
│
├── tools/                      # Инструменты агентов
│   ├── __init__.py
│   ├── file_tools.py
│   ├── excel_tools.py
│   ├── shell_tools.py
│   ├── web_tools.py
│   └── git_tools.py
│
├── workspace/                  # Рабочие файлы пользователей
│   └── tg_123456789/
│       ├── inbox/              # входящие файлы от пользователя
│       ├── output/             # результаты работы агента
│       └── temp/               # временные файлы (чистятся через 7 дней)
│
├── memory/                     # Память агента
│   ├── tg_123456789.json       # профиль пользователя
│   ├── sessions/               # горячая память (история диалогов)
│   │   └── tg_123456789.json
│   ├── decisions.log           # лог всех действий
│   └── vector_store/           # ChromaDB (Этап 5)
│
├── versions/                   # Резервные копии agent.py
│
├── web/                        # Web UI (Этап 4)
│   ├── app.py
│   └── templates/
│       └── index.html
│
└── tests/                      # Тесты (Этап 6)
    ├── test_core.py
    ├── test_tools.py
    └── fixtures/
        └── sample.xlsx
```

---

## Этапы разработки

---

### ЭТАП 0 — Фундамент
**Статус: готов ✓**

- [x] 0.1 Структура папок и файлов
- [x] 0.2 Защита саморедактирования
- [x] 0.3 Профили пользователей
- [x] 0.4 Рефакторинг конфигурации
- [x] **0.5 Память пользователя и онбординг**

#### 0.5 Память пользователя и онбординг
- [x] Автоочистка `workspace/{user_id}/temp/` при старте (файлы старше 7 дней)
- [x] Функция `identify_user()` — определяет пользователя по интерфейсу
  - CLI: из аргумента `--user` или вводится вручную при старте
  - Telegram: из `user_id` сообщения (Этап 4)
- [x] Проверка при старте: есть файл `memory/{user_id}.json`?
- [x] Если нет — режим онбординга:
  - Мира представляется и задаёт вопросы по одному
  - После разговора — API-вызов для структурирования профиля
  - Сохранение в `memory/{user_id}.json`
- [x] Если да — загрузка профиля и приветствие по имени
- [x] Команда `/whoami` — показать что агент знает о пользователе
- [x] Команда `/forget` — сбросить профиль, начать онбординг заново
- [x] Создание структуры `workspace/{user_id}/{inbox,output,temp}/` при старте
- [ ] Мира сама обновляет профиль в процессе работы → перенесено в **Этап 5**

**Результат этапа:** агент знает кто его пользователь, помнит между сессиями, создаёт правильную структуру папок.

---

### ЭТАП 1 — Класс Agent и Tool Calling
**Агент перестаёт советовать и начинает делать.**

#### 1.1 Рефакторинг: класс Agent
- [ ] Выделить класс `Agent` с методами: `run()`, `classify()`, `reply()`, `use_tool()`
- [ ] Конфиг агента загружается из `agents/{name}.json`
- [ ] Безопасность: `/evolve` доступен только в профиле `dev`

#### 1.2 Подключение инструментов (OpenAI function calling)
- [ ] Передавать `tools=` список доступных инструментов
- [ ] Обработчик `tool_calls` в ответе
- [ ] Цикл: агент вызывает инструменты пока не вернёт финальный текст

#### 1.3 Базовые инструменты (`tools/file_tools.py`)
- [ ] `list_files(path)` — список файлов в workspace пользователя
- [ ] `read_file(path)` — прочитать файл
- [ ] `write_file(path, content)` — записать файл
- [ ] Безопасность: все пути ограничены `workspace/{user_id}/`

#### 1.4 Инструмент выполнения кода (`tools/shell_tools.py`)
- [ ] `run_python(code, timeout=30)` — выполнить в подпроцессе

---

### ЭТАП 2 — Конклав (мультиагентность)
- [ ] Класс `Conclave`, оркестратор
- [ ] Роутер: классификатор задач
- [ ] Специалисты: excel, coder, planner, critic
- [ ] Саморасширение: Альфа создаёт нового агента под новую задачу

---

### ЭТАП 3 — Excel и сценарий мамы
- [ ] Excel инструменты (openpyxl)
- [ ] Сценарий "показания счётчиков" — полный флоу

---

### ЭТАП 4 — Telegram Bot + Web UI
- [ ] Telegram Bot (python-telegram-bot)
- [ ] Web UI (FastAPI + HTML)
- [ ] Деплой (systemd + nginx)

---

### ЭТАП 5 — Долгая память + Облако
- [ ] Журнал решений (`memory/decisions.log`)
- [ ] Адаптация под пользователя
- [ ] **rclone + Google Drive** для синхронизации `memory/`
  - `RCLONE_REMOTE=gdrive:mira_memory` в `.env`
  - `/cloud sync` — отправить на Drive
  - `/cloud restore` — восстановить с Drive
- [ ] Векторная память (ChromaDB, опционально)

---

### ЭТАП 6 — Тесты
- [ ] Unit-тесты ядра, инструментов, Excel
- [ ] Интеграционный тест сценария мамы
- [ ] Evolve запускает тесты перед записью кода

---

### ЭТАП 7 — Деплой и мониторинг
- [ ] Алерт в Telegram если агент упал
- [ ] Бэкап `memory/` раз в сутки через rclone
- [ ] Nginx + HTTPS

---

## Порядок работы

1. Берём следующий незакрытый пункт
2. Я пишу код, ты запускаешь
3. Говоришь что работает, что нет — итерируем
4. Ставим галочку, идём дальше

**При каждом новом сеансе** — закинь актуальный `agent.py` и этот `PLAN.md`.

---

## Бюджет API (примерная оценка)

| Сценарий | Вызовов на запрос | Стоимость (DeepSeek V3 Flash) |
|---|---|---|
| Беседа | 1 | ~$0.0001 |
| Excel задача | 1–2 | ~$0.0002 |
| Сложная задача | 3 | ~$0.0005 |
| 100 запросов мамы в месяц | — | **~$0.02–0.05** |

---

## Текущий статус

```
[x] Базовый агент
[x] ЭТАП 0.1 — Структура папок
[x] ЭТАП 0.2 — Защита /evolve
[x] ЭТАП 0.3 — Профили
[x] ЭТАП 0.4 — Конфигурация
[x] ЭТАП 0.5 — Память + Онбординг
[ ] ЭТАП 1   — Tool Calling          ← СЛЕДУЮЩИЙ
[ ] ЭТАП 2   — Конклав
[ ] ЭТАП 3   — Excel
[ ] ЭТАП 4   — Telegram Bot + Web UI
[ ] ЭТАП 5   — Долгая память + rclone
[ ] ЭТАП 6   — Тесты
[ ] ЭТАП 7   — Деплой
```
